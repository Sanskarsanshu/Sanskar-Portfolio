# GitHub Personal Access Token (classic)
# Required to bypass the 60 requests/hour unauthenticated API rate limit.
# You can generate one here: https://github.com/settings/tokens
# No scopes/permissions are required for public data, just generate the token.
GITHUB_TOKEN=your_personal_access_token_here
